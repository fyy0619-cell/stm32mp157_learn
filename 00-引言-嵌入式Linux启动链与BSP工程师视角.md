# 00 · 引言 —— 嵌入式 Linux 启动链与 BSP 工程师视角

> 目标:理解一次 STM32MP157 从上电到出现 shell 提示符,中间经过哪些环节;每个环节对应哪种类型的问题;BSP 工程师日常在哪几层作战。
> 阅读时长:约 10 分钟

---

## 一、为什么面试官爱问"启动流程"?

嵌入式 Linux 岗位面试,启动流程几乎是必问题。原因不是它多难,而是它**能一句话检验出候选人有没有做过真实项目**:

- 只跑过应用的人:讲不清楚 U-Boot 之前发生了什么
- 只调过应用的人:能讲 U-Boot,但对 Kernel 挂 rootfs 的过程一知半解
- 真做过 BSP 的人:能定位到"上电后的第 N 毫秒卡在哪层"

本笔记通篇的坑,几乎每一个都可以精确地定位到启动链的某一层。掌握了分层视角,后面所有章节读起来都会像"填空"。

---

## 二、STM32MP157 完整启动链

STM32MP157 是**大小核异构 SoC**(Cortex-A7 双核 + Cortex-M4),启动流程比纯 A 核 SoC 稍复杂。核心链路如下:

```
┌─────────────────────────────────────────────────────────────┐
│  Stage 0  片上 BootROM (固化在 SoC 里,不可改)                │
│           ↓  从 boot pin 决定的启动介质(SD/eMMC/USB/UART)    │
│           ↓  加载 FSBL 到 SYSRAM                             │
├─────────────────────────────────────────────────────────────┤
│  Stage 1  FSBL = TF-A BL2 (First Stage Bootloader)          │
│           ↓  初始化 DDR、PMIC (STPMIC1)、时钟树             │
│           ↓  加载 SSBL 到 DDR                                │
├─────────────────────────────────────────────────────────────┤
│  Stage 2  OP-TEE (可选,可信执行环境)                         │
│           ↓  安全世界与非安全世界隔离                        │
├─────────────────────────────────────────────────────────────┤
│  Stage 3  SSBL = U-Boot (Second Stage Bootloader)           │
│           ↓  bootcmd 自动执行                                │
│           ↓  从 MMC/TFTP 加载 uImage 和 dtb                  │
│           ↓  bootm 跳转到内核                                │
├─────────────────────────────────────────────────────────────┤
│  Stage 4  Linux Kernel (vmlinux)                             │
│           ↓  解析 dtb → 建立设备模型                         │
│           ↓  driver probe → 挂载 rootfs                     │
├─────────────────────────────────────────────────────────────┤
│  Stage 5  init 进程 → 用户空间 → shell                       │
└─────────────────────────────────────────────────────────────┘
```

**注意**:M4 核的加载走另一条支路(通过 Linux 用户态的 `remoteproc` 框架启动),不影响 A 核 rootfs 挂载。

---

## 三、每层能被哪类日志识别?

面试官经常抛一段崩溃日志问"卡在哪层",本节就是训练这个反应。速查表:

| 日志特征 | 卡在的层 | 快速原因猜测 |
|---|---|---|
| 串口无输出 / 停在 `NOTICE:` | Stage 0/1 (BootROM / TF-A) | 电源/DDR 时序/启动介质 |
| `stpmic1 ... Unable to read` | Stage 1 或 4 (PMIC 通信) | I2C 接线 / PMIC 方案不匹配 |
| `U-Boot 2020.01` 且能打字 | Stage 3 | Bootloader 已进 shell,后续看 bootcmd |
| `Wrong Image Format for bootm` | Stage 3 | bootm 参数错 / 地址处内容非合法 image |
| `Starting kernel ...` 之后无输出 | Stage 3→4 过渡 | 内核 console 参数错 / 编译时忘配 CONFIG_EARLY_PRINTK |
| `Booting Linux on physical CPU 0x0` | Stage 4 已启动 | 内核跑起来了 |
| `VFS: Cannot open root device` | Stage 4 末尾 | rootfs 挂不上(存储/网络/驱动) |
| `Kernel panic - not syncing: VFS:` | Stage 4→5 过渡失败 | init 找不到 |
| `login:` 出现 | Stage 5 成功 | 到用户空间了 |

**这张表建议背下来**——面试时对方一贴日志,你能立刻说"这看起来是 Stage X 的问题,通常有 A/B/C 三种原因",这比"我看看啊..."专业十倍。

---

## 四、BSP 工程师的日常战场

不同岗位的开发者关心不同层:

| 岗位 | 主要战场 | 次要战场 |
|---|---|---|
| 应用开发 | Stage 5 (用户空间) | — |
| 驱动开发 | Stage 4 (Kernel + DTS) | Stage 3 (为驱动传参) |
| **BSP 工程师** | **Stage 1-4 全链路** | Stage 5 (联调) |
| **AI BSP / NPU** | **Stage 4 (NPU 驱动) + Stage 5 (推理框架适配)** | Stage 1-3 (给 NPU 供电/时钟) |
| 硬件工程师 | Stage 0-1 (原理图/PMIC/DDR) | — |

**你如果目标是 AI BSP / NPU 方向**,需要在 Stage 4 特别下功夫——因为 NPU 从驱动角度看,本质就是一个"带 DMA 的加速器外设":

- 需要**内核驱动**注册 char/misc device,提供给用户态的 ioctl
- 需要**设备树**描述 NPU 的中断号、寄存器 base、时钟、复位、供电
- 需要**上电时序**(通常 PMIC 单独一路 buck)
- 需要**推理框架**(TFLite Micro / TIDL / Rockchip RKNN)在用户态调用驱动

**本仓库虽然对象是 STM32MP157(没有 NPU),但你能把 stmmac 网卡驱动的移植走通,同样的方法论套到 NPU 驱动上是完全通用的。** 这是本仓库对 NPU 岗位面试的核心价值。

---

## 五、必须记住的三个坐标系

BSP 排障离不开三个坐标系,新手混淆是常见错误来源:

### 1. 空间坐标:U-Boot 世界 vs Kernel 世界

- U-Boot 里网卡能 `tftp` 成功 ≠ Kernel 里网卡能用
- U-Boot 里 `ping` 通 ≠ Linux 里 `ping` 通
- 因为 **U-Boot 和 Kernel 用的是完全独立的驱动栈**

### 2. 时间坐标:上电 → 用户空间的顺序不可颠倒

- rootfs 挂不上 → 不可能有 shell
- 网卡驱动没起来 → NFS rootfs 不可能挂上
- 所以看到"没 shell"的第一反应是"往前追,是哪一步没通过"

### 3. 层次坐标:硬件 → 内核 → 用户态

- 硬件问题(PHY 没供电) → 用软件永远修不好
- 内核问题(DTS 没使能) → 改用户态配置没用
- 用户态问题(NFS 配错) → 别去动内核

**遇到问题第一件事:在这三个坐标系里,先给现象定位,再动手。** 直接上手改配置试错是最低效的方式。

---

## 六、本笔记如何映射到岗位面试

面试嵌入式 Linux / BSP 岗位,常见提问模式:

| 面试问题 | 本笔记对应章节 | 展示的能力 |
|---|---|---|
| "讲一下 Linux 启动流程" | 本章 (00) | 全链路认知 |
| "板子启动不了,你怎么排查?" | 01, 02 | 分层定位方法论 |
| "DTS 和驱动是怎么关联的?" | 03 | 内核设备模型理解 |
| "NFS 挂不上,可能是什么原因?" | 04 | 网络子系统排障 |
| "遇到过什么棘手的编译问题?" | 05 | 工具链/glibc 深度 |
| "为什么你 Ubuntu 用 20.04 不用最新?" | 05 | 工程判断力 |
| "犯过什么低级错误?学到了什么?" | 06 | 反思能力(HR 爱问) |

**07 章会把每个问题的 STAR 话术模板都写出来**,面试前重点看那一章。

---

## 七、下一步

后续 7 章按启动链自底向上展开。建议按顺序读,每章末尾都有"跟本章相关的面试问题",训练现场表达。

**从 01 章开始 → [01-Bootloader层-U-Boot命令语法与环境变量.md](./01-Bootloader层-U-Boot命令语法与环境变量.md)**
