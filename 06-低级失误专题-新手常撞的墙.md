# 06 · 低级失误专题 —— 新手常撞的墙

> 本章记录**技术含量不高但很浪费时间**的失误。写下来的目的不是自嘲,而是:
> 1. 面试时被问"你犯过什么低级错误",能坦然说出来 + 学到什么(HR 面爱问,考察反思能力)
> 2. 提醒自己:嵌入式开发一多半时间在"环境"上,减少这类错误就是提高效率
> 阅读时长:约 10 分钟

---

## 失误 6-1:登录提示符敲命令

### 现象

```
Welcome to alientek STM32MP157
ATK-stm32mp1 login: ifconfig -a
Password:
Login incorrect
ATK-stm32mp1 login: ifconfig
Password:
Login incorrect
```

登不上。反复重试都不对。

### 根因

`login:` 后面要输**用户名**,不是命令。输 `ifconfig`,系统把 `ifconfig` 当用户名去查,查不到再要密码;密码乱输就 login incorrect。

正确做法:
```
ATK-stm32mp1 login: root
Password:               ← 回车 (ATK 默认无密码)
#                       ← 出现 # 才是 shell
```

### 教训

**任何提示符都有它期待的输入类型**——login: 期待用户名、Password: 期待密码、# 或 $ 期待命令、`>` 通常是子 shell 或未闭合语句。**看清提示符再敲**,是嵌入式开发的第一条素养。

### 岗位映射

HR 面话术:
> "刚接触嵌入式 Linux 时犯过一个低级错误——在 login 提示符敲命令,登不上纠结半天。后来意识到不同提示符期待不同输入类型,是操作系统人机交互的基本约定。这个教训让我养成一个习惯:动手前先看提示符和光标位置,判断当前上下文。"

---

## 失误 6-2:`sudo make` 编译 Buildroot 污染权限

### 现象

见 [05 章坑 5-1](./05-交叉编译环境-Buildroot新老系统兼容性.md#坑-5-1sudo-make-触发you-should-not-run-configure-as-root)

### 教训

**"没权限就 sudo"是新手的通病**。正确思路:先问自己"这个操作真的需要 root 吗?"

区分:
- **本地编译**:99% 不需要 root
- **写系统目录**(/etc, /usr):需要
- **访问硬件**(/dev/sd*、GPIO 直读):需要
- **改用户 shell 环境**(~/.bashrc):不需要,自己的家目录

**遇到 permission denied,先看是不是自己文件、家目录、权限被前一个 sudo 命令污染**,而不是无脑 sudo。

---

## 失误 6-3:`menuconfig` 终端窗口太小

### 现象

```bash
$ make menuconfig
scripts/kconfig/mconf  Kconfig
Your display is too small to run Menuconfig!
It must be at least 19 lines by 80 columns.
```

### 教训

`menuconfig` 是 ncurses TUI,需要终端至少 **19 行 × 80 列**。MobaXterm / xterm / gnome-terminal 默认窗口可能不够。

**修复**:
- 直接拖大窗口 / F11 全屏
- 或 `stty size` 查看当前尺寸

**顺带认识几个 kconfig 前端**:
- `menuconfig` —— ncurses TUI (最常用)
- `nconfig` —— 更好看的 ncurses TUI
- `xconfig` —— Qt GUI (需要 X11)
- `gconfig` —— GTK GUI
- `oldconfig` —— 交互式命令行(升级 config 用)
- `defconfig` / `<板>_defconfig` —— 加载预设

---

## 失误 6-4:`sed` 命令少参数

### 现象

```bash
$ sed -i 's/#elif HAVE_LIBSIGSEGV && SIGSTKSZ < 16384/#elif HAVE_LIBSIGSEGV/'
sed: 没有输入文件
```

复制粘贴的时候把文件路径截掉了。

### 教训

**长命令拷粘时,滚动条容易截断**。防御姿势:
1. 复制完先在编辑器里粘贴一次,肉眼确认完整
2. 或者在 shell 里粘贴后按 Home 键翻到开头,End 键翻到结尾,快速目视
3. **正确 sed 语法**:`sed -i '<expr>' <file>`——文件名是必需的位置参数

### 岗位映射

**这类错误反映"命令语法直觉"是否熟练**。老手看到 `sed -i 's/.../.../'` 会瞬间意识到"缺文件",新手才要看错误信息才发现。**多敲多熟**。

---

## 失误 6-5:改了 .config 但没重编 / 编了但没部署

### 场景

改了 `CONFIG_STMMAC_ETH=y`,但:
- 忘了 `make` 重编
- 编了但忘了拷贝新 uImage 到 tftpboot
- 拷贝了但板子上 U-Boot 拉的是旧地址(比如 MMC 上的旧 uImage,不是 TFTP 上的新的)

结果:改了半天配置,**跑的还是旧 kernel**,现象一模一样。

### 教训

嵌入式 Linux 修改后必须**验证"改动真的生效"**:

1. **改了源码/config** → `ls -la` 看 .config 时间戳
2. **重编** → 看 uImage 时间戳
3. **拷贝** → 看 tftpboot 里的 uImage 时间戳
4. **烧录/加载** → 板子起来后 `uname -a` 看编译时间

**四个时间戳应该一路刷新**,任何一个没更新说明流水线断在那一步。

### 面试话术

> "早期做嵌入式老犯一个错:改了 config 但忘了重编,或者编了忘了部署,结果调试半天现象没变,还以为是配置改错了。后来我养成了一个习惯:每次修改后核对四个时间戳——源文件 → 中间产物 → 部署位置 → 运行时 uname 的 build time。四个时间一路刷新才算真部署了。这个流水线思维在 CI/CD 里也一样,任何一步没生效都会导致'幽灵 bug'。"

---

## 失误 6-6:U-Boot 里 `bootcmd` 没 `saveenv` 就 reset

### 现象

在 U-Boot 里 setenv 完 bootcmd,直接 reset,发现改动没保留。

### 教训

**U-Boot 环境变量修改必须 `saveenv` 才写入持久化存储**。setenv 只改内存里的副本,reset 就没了。

顺带一个进阶点:`saveenv` 依赖 U-Boot 编译时正确配置了 `CONFIG_ENV_IS_IN_MMC / _FLASH / _EEPROM`,存到哪个介质。如果 env 分区没规划好,`saveenv` 会失败或写到错误位置。

---

## 失误 6-7:`ext4load` 用错分区号

### 现象

```
ext4load mmc 1:2 c2000000 uImage
** File not found /uImage **
```

分区号 `1:2` 意思是"MMC 设备 1 的第 2 分区"。如果:
- 你的 rootfs 在 partition 3 而不是 2
- 或 MMC 设备编号不是 1(比如 SD 卡是 0)

就找不到文件。

### 教训

用 `mmc list` / `mmc info` 查设备号,`ls mmc 1` 查分区列表,先摸清结构再敲命令。

---

## 失误 6-8:虚拟机挂起后 NFS/TFTP 突然不通

### 现象

前一天调好的环境,第二天挂起 → 恢复,板子突然连不上服务器。

### 根因

虚拟机挂起时的网络状态**不保证恢复后一致**:
- 桥接的物理网卡可能变了(有线 → 无线)
- DHCP 租约可能过期,分到新 IP
- Ubuntu 的 NetworkManager 可能"帮"你重连,IP 变了

### 教训

**开发环境用静态 IP,不用 DHCP**。虚拟机的静态配置(netplan 或者 /etc/network/interfaces)一次配好,挂起恢复不受影响。

---

## 二、通用防御姿势清单(收藏)

从这堆坑里提炼出的通用规则:

| 规则 | 具体做法 |
|---|---|
| 看提示符 | login: / # / > 都不一样 |
| 少用 sudo | 先问自己"真的需要 root 吗" |
| 拷粘检查 | 长命令粘贴后,End 键翻到结尾看完整 |
| 时间戳追踪 | 源文件 → 编译产物 → 部署 → uname,四步同步刷新 |
| 静态 IP | 开发网络配静态,不依赖 DHCP |
| 变量持久化 | U-Boot setenv 后 saveenv,shell 变量导出后 source |
| 提前 && | shell 脚本里 `cmd1 && cmd2` 比 `cmd1;cmd2` 安全 |
| 保留原状 | 改配置前 cp 备份,便于回滚 |

---

## 本章相关的面试问题(HR 面向)

HR 面/软素质面常问反思类问题:

1. **你在实习/项目里犯过什么低级错误?学到了什么?**
2. **遇到反复不生效的 bug 你会怎么排查?**
3. **你怎么平衡"快速试错"和"防御性编程"?**
4. **同事的代码风格跟你不一样,你会主动改吗?**

**第 1 题标准答案框架**:
> "低级错误 → 引发的实际后果 → 我怎么理解这个错误的更深层原因 → 学到的通用规则 → 之后怎么避免"

**别只讲"犯错本身",要讲"这个错让我建立了什么长期习惯"**。

---

## 下一章

**面试话术专题** —— 把本仓库前 6 章的技术积累翻译成"能面试的能力表达" → [07-面试话术-如何把这些经历讲成能力.md](./07-面试话术-如何把这些经历讲成能力.md)
